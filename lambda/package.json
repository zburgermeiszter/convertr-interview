{
  "name": "picture-upload-lambda",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "audit": "bun audit",
    "typecheck": "tsc --noEmit",
    "build": "bun build --target=node --sourcemap=inline --production --outfile=./dist/index.mjs src/index.ts",
    "watch": "bun run --watch src/index.ts"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.156",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.911.0",
    "file-type": "^21.0.0"
  }
}
